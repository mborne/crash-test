apiVersion: v1
kind: Service
metadata:
  name: {{ include "crash-test.fullname" . }}
  labels:
    {{- include "crash-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crash-test.selectorLabels" . | nindent 4 }}
