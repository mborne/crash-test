apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crash-test.fullname" . }}-test-connection"
  labels:
    {{- include "crash-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crash-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
